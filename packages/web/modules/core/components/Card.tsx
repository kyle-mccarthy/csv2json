import { styled } from "../theme";

const Card = styled("div", {
  background: "$slate3",
  display: "flex",
  width: '100%',
  padding: "$6 $4",
  borderRadius: "$sm",
  flexDirection: "column",
});

export default Card;
